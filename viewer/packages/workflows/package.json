{
  "name": "@bim-open-toolkit/workflows",
  "version": "0.1.0",
  "description": "Pure result adapters and recipes for the review workflows: input tables in, typed results, rules, sets, overlays and views out.",
  "license": "MIT",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist",
    "README.md"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "test": "vitest run"
  },
  "dependencies": {
    "@bim-open-toolkit/model": "0.1.0"
  }
}
