// Public API of @bim-open-toolkit/demos: the gallery's contracts, so a demo written elsewhere can
// register against the same shapes. The fixture server has its own entry (`src/server`), and the
// pages are built by Vite, not imported.
export * from './gallery/contracts.js';
