# Orthographic projection checkpoint

G1.1 read and unchanged. Parallel Wave and Platonic Coder applied. Owns core `src/viewer.ts`/`test/viewer.test.ts`; visualization `src/projection.ts`, `test/projection.test.ts`, `examples/features/projection.ts`, and this checkpoint. State: focused tests and strict scoped demo typecheck verified. No processes running.

Viewer keeps its existing readonly perspective `camera`. `renderCamera` returns the camera currently used for drawing/picking; `setRenderCamera(camera)` borrows an alternate camera and null restores the default. Borrowed cameras are not disposed by Viewer. Resize updates both the default perspective aspect and an override; orthographic resize preserves vertical span, horizontal center and zoom while adapting horizontal span to viewport aspect. Generic Camera overrides retain host-owned projection matrices. Dispose clears the override reference.

Pure `fitOrthographicView(bounds,{view,aspect,padding?})` supports top/front/side/isometric. It projects box extents onto camera axes and fits both viewport dimensions, generating plain camera pose/frustum/near/far data. Top uses a nonsingular up vector. The demo consumes this data in a Three OrthographicCamera with view, zoom and fit buttons, then restores the previous camera on return/unmount.

While orthographic is active, capture-phase handlers suppress pointer motion and wheel orbit events without disposing host controls or subscriptions. Pointer-down/up remain available for picking. Perspective navigation remains unchanged after restoration. Host picking must use `viewer.renderCamera`; the coordinator owns that small integration change. Host baseline Fit remains perspective-specific; use the demo's Fit orthographic button. Free orthographic orbit/pan and seamless projection-switch scale matching remain deferred.

Focused commands use package-local `../../node_modules/.bin/vitest.cmd run <test> --maxWorkers=1 --cache=false`: core `test/viewer.test.ts` passed 7 tests in 991 ms; visualization `test/projection.test.ts` passed 5 tests in 780 ms (September 7, 2026). Checks cover default restoration, borrowed perspective/orthographic resize, zero viewport handling and all eight box corners for four views at portrait/landscape aspects. After coordinator core build, `tsc.cmd --noEmit --strict --skipLibCheck --target ES2022 --module ES2022 --moduleResolution bundler --lib ES2022,DOM examples/features/projection.ts` passed. Coordinator changed host picking to renderCamera. No shared builds run by this track. Commit pending.
