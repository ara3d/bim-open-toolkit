{
  "name": "@bim-open-toolkit/model",
  "version": "0.1.0",
  "description": "Pure data contracts and operations: identity, coordinates, sets, style rules, edits, view state, document slices, schema combinators, Result, facts, Table, mesh POD, Feature, Command, StateSlice and Event contracts.",
  "license": "MIT",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist",
    "README.md"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "test": "vitest run"
  }
}
