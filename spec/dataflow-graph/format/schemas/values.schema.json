{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://spec.ara3d.com/dataflow-graph/format/0.1.0/values.schema.json",
  "title": "DataFlow Graph values layer",
  "description": "Per-node parameter values. Every value is the canonical string form of the parameter (format.md section 4); whether each string parses as its declared parameter kind is catalog validity, not expressible here.",
  "type": "object",
  "patternProperties": {
    "^[A-Za-z0-9_-]+$": {
      "type": "object",
      "patternProperties": {
        "^[A-Za-z][A-Za-z0-9]*$": {
          "type": "string"
        }
      },
      "additionalProperties": false
    }
  },
  "additionalProperties": false
}
