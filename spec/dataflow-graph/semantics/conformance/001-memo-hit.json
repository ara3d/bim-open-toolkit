{
  "part": "semantics",
  "case": "001-memo-hit",
  "description": "Re-evaluating an unchanged graph executes nothing: every node is a memo hit (semantics.md sections 4-5).",
  "input": {
    "document": {
      "formatVersion": "0.1.0",
      "structure": {
        "nodes": [
          { "id": "a", "kind": "test.const", "version": 1 },
          { "id": "p", "kind": "test.probe", "version": 1 }
        ],
        "edges": [
          { "from": "a.out", "to": "p.in" }
        ]
      },
      "values": {
        "a": { "kind": "Integer", "value": "42" }
      }
    },
    "steps": [
      { "action": "evaluate" },
      { "action": "evaluate" }
    ]
  },
  "expect": {
    "steps": [
      { "executions": { "a": 1, "p": 1 } },
      { "executions": { "a": 0, "p": 0 } }
    ],
    "outputs": {
      "p.out": { "kind": "Integer", "value": 42 }
    }
  }
}
