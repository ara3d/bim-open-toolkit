{
  "formatVersion": "0.1.0",
  "layout": {
    "slim": { "x": 320, "y": 40 },
    "warehouse": { "x": 40, "y": 40 }
  },
  "structure": {
    "edges": [
      { "from": "warehouse.table", "to": "slim.table" }
    ],
    "nodes": [
      { "id": "slim", "kind": "table.project", "version": 1 },
      { "id": "warehouse", "kind": "duck.query", "version": 1 }
    ]
  },
  "values": {
    "slim": { "columns": "Customer, Product, Revenue" },
    "warehouse": {
      "path": "{SAMPLES}/sample.duckdb",
      "sql": "SELECT c.Name AS Customer, p.ProductName AS Product, o.OrderDate AS OrderDate, CAST(o.Quantity * p.UnitPrice AS DOUBLE) AS Revenue FROM Orders o JOIN Products p ON o.ProductId = p.ProductId JOIN Customers c ON o.CustomerId = c.CustomerId ORDER BY Revenue DESC"
    }
  }
}
