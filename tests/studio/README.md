# tests/studio

NUnit project for `src/studio`: `BimOpenFlow.Studio.Tests` covers the Studio
hosting of the flow host and the Ask agent loop. The Studio scripts in
`Ara3D.Studio.BimTools` run only inside Ara 3D Studio and have no tests here.
