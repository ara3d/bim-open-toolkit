# tests/mcp

NUnit projects for the two MCP servers in `src/mcp`. `Ara3D.Ifc.Mcp.Tests`
includes stdio end-to-end tests that launch the server as a subprocess;
`BimOpenFlow.Mcp.Tests` drives the graph tools against an in-process host.
