# apps

Runnable desktop applications built on the toolkit. Not NuGet packages.

| Project | Role |
|---|---|
| `Ara3D.BimOpenSchema.Browser` | A WPF data-grid viewer for `.bos` archives with glTF and Excel export. Ara 3D Studio launches it from a `browser` folder beside its own executable. Targets `net10.0-windows`. |
