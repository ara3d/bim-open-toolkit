# Draft, not built

Track X (React bindings and review application) was stopped at the user's request before it verified anything. These files are its draft as it stood: they do not typecheck yet and are outside the package's `src` and `test` so the gates ignore them. A resumed Track X moves them back into `src/`, finishes them, and deletes this folder.
