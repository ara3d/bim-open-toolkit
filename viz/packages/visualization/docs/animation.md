# Animation checkpoint

Wave4, V1/G1 acknowledged; Parallel Wave and Platonic Coder applied. Owns `src/animation.ts`, `test/animation.test.ts`, `examples/features/animation.ts`, and this checkpoint. State: verified by scoped tests/typecheck. No processes or shared output mutation. Commit: `1ee7b63e62693e2fc3579efecfaee43aa543ceb8` (post-commit checkpoint line for integration).

Playback is plain readonly state. `createPlayback` establishes duration, positive rate, looping and initial timestamp. `advancePlayback`, `playPlayback`, `pausePlayback`, `seekPlayback`, `setPlaybackRate`, and `resetPlayback` consume supplied monotonic timestamps in seconds. No global clock or scheduler exists in the public helper. Endpoints clamp in nonlooping mode; looping wraps across multiple periods. Pause and rate changes preserve elapsed playback; resuming excludes paused time. Seek clamps its explicit position. Reset preserves rate/loop configuration but pauses at zero. `sampleCircularTranslation` depends only on time and returns a horizontal circle displacement starting at zero.

`animationDemo` identifies one bound object once, caches original data and scene-scale radius, and updates only that logical object's render representations per active frame. It never sends an all-model snapshot or rescans metadata per frame. A RAF exists only during playback. Play/pause/seek/speed/reset controls show time progress. Reset/unmount restore the saved source placement; animation never adds permanent edit history. The operation is explicitly synthetic movement, not a construction schedule. This initial example animates an existing object; avatar/proxy and delivered/accepted/installed schedule examples remain deferred.

Checks (September 7, 2026) from visualization using `../../node_modules/.bin/`: `vitest.cmd run test/animation.test.ts --maxWorkers=1 --cache=false` passed 4 tests in 392 ms; `tsc.cmd --noEmit --strict --skipLibCheck --target ES2022 --module ES2022 --moduleResolution bundler --lib ES2022,DOM examples/features/animation.ts` passed. Pure tests exercise pause/resume without a jump, rate continuity, end/restart/reset, multi-period looping, seek equivalence and input validation. Real Snowdon animation/display cost remains a coordinator gate. No processes running. Commit pending.
